# Only the backend goes into the image. Everything else is excluded so the
# build context stays small and the image doesn't bloat.

# Frontend
apps/web/

# Docs / repo meta
docs/
scripts/
README.md
DEPLOYMENT.md

# Replit-specific
.replit
replit.nix
replit.md
.config/
.upm/
.local/
.agents/
attached_assets/
screenshots/
# NOTE: do NOT add `main.py` here — docker's .dockerignore patterns
# match anywhere in the tree and will strip apps/api/app/main.py
# (the actual FastAPI entrypoint) out of the build context.
# The root main.py is harmless inside the image.

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pythonlibs/
.pytest_cache/
.cache/

# Node (just in case)
node_modules/
dist/
.vite/

# Env / secrets — NEVER ship .env into the image
.env
.env.local
*.local
apps/api/.env
apps/api/.env.*
apps/api/logs/

# Editor / OS
.DS_Store
.idea/
.vscode/
.git/
.gitignore
